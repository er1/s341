UML 2.0 Activity Diagram representing the process expressed by UC 11 (Generate Advanced Schedule). It shows the process when a user is logged or not. Scheduled are then generated and if matched are found user can accept a choice of schedule. Once the schedule passes all validation it is officially saved and the process ends. The process only ends when user select a valid schedule and register for it.